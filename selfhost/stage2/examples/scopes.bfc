void main() {
    cell value = 'A';
    {
        cell value = 'B';
        output(value);
    }
    output(value);
    {
        cell temporary = 'C';
        output(temporary);
    }
    // This declaration reuses and zeroes the completed block's tape cell.
    cell zero;
    output(zero);
}
