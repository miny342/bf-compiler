void main() {
    cell value = input();
    if (!value) {
        output('Z');
    } else {
        while (value != 0) {
            if (value != 0) {
                output(value);
            } else {
                output('X');
            }
            value -= 1;
        }
    }
}
