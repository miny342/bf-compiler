# Brainfuck 実行仕様

この文書は、本プロジェクトがコンパイラの出力先として採用する Brainfuck
方言を定義する。

## プログラム

プログラムは任意の byte 列である。次の 8 種類の ASCII byte だけを命令として
解釈し、それ以外はコメントとして無視する。

| 命令 | 動作 |
| --- | --- |
| `>` | データポインタを 1 セル右へ移動する |
| `<` | データポインタを 1 セル左へ移動する |
| `+` | 現在のセルを 1 増やす |
| `-` | 現在のセルを 1 減らす |
| `.` | 現在のセルを 1 byte 出力する |
| `,` | 1 byte 入力し、現在のセルへ格納する |
| `[` | 現在のセルが 0 なら、対応する `]` の直後へ移動する |
| `]` | 現在のセルが 0 でなければ、対応する `[` の直後へ移動する |

実行前にすべての括弧の対応を検査する。対応しない `[` または `]` がある
プログラムは構文エラーであり、一切実行しない。エラー位置は元のプログラムの
0 始まりの byte offset で表す。

## オプションのテキスト圧縮形式（BFCRLE v1）

Rust版コンパイラの `--compressed-bf` は、先頭に `@BFCRLE1;` を付けた
圧縮テキストを出力する。省略時は従来の通常BFを出力する。
interpreterはこの先頭ヘッダを自動判別する。ヘッダなしでは従来通り数字も
コメントであり、`+163` は `+` 1回として実行する。

圧縮形式では `+`、`-`、`<`、`>` の**直後**のASCII十進数字列を総反復数として
解釈する。例：`[+163>23-<16]`。数字を省略すると1回。先頭ゼロは許すが、
反復数0、`isize::MAX`を超える数、および展開後の総命令数がその上限を超える
入力は構文エラーとする。空白やmarkerをまたいで数字を読まない。
`[],.` は圧縮しない。先頭の `@BFCRLE` は形式識別用に予約し、未知versionは
エラーにする。ヘッダはBOMや空白より前、必ずbyte offset 0に置く。

通常BFへの全面展開をせず、直接FastIRへ読み込む。実行カウンタ、profile mapの
範囲・BF identityは展開後の命令列に基づき、同じ内容の通常BFと共有できる。
エラー位置は圧縮ファイル内のbyte offsetであり、圧縮runの途中でのテープ境界
超過は、そのrunの命令記号の位置を報告する。

`--embed-profile` と併用可能。圧縮ヘッダの後に `@BFCDBG1;` とsite定義を置き、
`@P123;` markerは数字列と分離する。site境界をまたぐrunはmarker挿入時に分割する。
圧縮出力は通常の外部BF処理系では正しく実行できないため、その場合は
`--compressed-bf` を付けずに生成する。セルフホスト版コンパイラの出力は変更しない。

## セルとテープ

- セルは符号なし 8 bit 整数で、値域は `0..=255` とする。
- `+` と `-` はそれぞれ wrapping 加算・減算である。
- テープは 30,000 セルで、実行開始時はすべて 0 とする。
- データポインタはセル 0 から開始する。
- テープは循環しない。両端を越える移動は実行時エラーとする。

## 入出力

- 入出力は文字列ではなく byte 列として扱う。
- `.` は現在のセルをちょうど 1 byte 出力する。
- `,` は入力をちょうど 1 byte 消費する。
- 入力が EOF のとき `,` は現在のセルへ 0 を格納する。
- 改行コードや文字エンコーディングの変換は行わない。

したがって入力中の NUL byte と EOF は、BF プログラムからは区別できない。

## エラー

処理系は少なくとも次のエラーを区別する。

- 対応しない開き括弧
- 対応しない閉じ括弧
- テープ左端の超過
- テープ右端の超過
- ホスト環境における入出力エラー

セルのオーバーフローおよびアンダーフローは正常動作である。
